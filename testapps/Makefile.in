CC          = @CC@
CXX         = @CXX@
FC          = @FC@
MPICC       = @MPICC@

BINDIR      = ../src

OBJS        = foo.o bar.o dum.o main.o
OBJSCPP	    = classes.o
EXTRA_INC   = -I../instcode
EXTRA_LIBS  = -lm

TARGETS  = cTest cppTest htcoef dynTest sgbench

all: $(TARGETS) 

%.o: %.c
	$(CC) -g -c -o $@ $< 

%.o: %.C
	$(CXX) -g -c -o $@ $< 

cTest: $(OBJS)
	$(CC) -g -o $@ $(OBJS)

cppTest: $(OBJSCPP)
	$(CXX) -g -o $@ $(OBJSCPP)

htcoef:
	$(FC) -g -o $@ htcoef.f

dynTest:
	$(CC) -g -o $@ dynTest.c

sgbench:
	$(CC) -g -o $@ sgbench.c $(EXTRA_INC) $(EXTRA_LIBS)

clean: 
	rm -f *.o $(TARGETS) 

